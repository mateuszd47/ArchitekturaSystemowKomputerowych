import React from "react";
import Inputs16bit from "./Inputs16bit";

const Main = (mov) => {
	// const {mov} = Inputs16bit(sub)
	return (
		<main>
			<Inputs16bit />
		</main>
	);
};

export default Main;
